#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir//bin/bash" ]; then
  "$basedir//bin/bash"  "$basedir/../electron-prebuilt/run.bat" "$@"
  ret=$?
else 
  /bin/bash  "$basedir/../electron-prebuilt/run.bat" "$@"
  ret=$?
fi
exit $ret
